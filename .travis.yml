language: java

jobs:
  include:
    - language: node_js
      node_js: 9
      install:
        - cd src/main/front
        - npm ci --production
      script:
        - npm run build
    - language: java
      jdk: openjdk8