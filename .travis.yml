language: java

jobs:
  include:
    - language: node_js
      node_js: 9
      install:
        - cd src/main/front
        - npm install --production
      before_script:
        - npm run test
      script:
        - npm run build:react
    - language: java
      jdk: openjdk8
      before_install:
        - chmod +x mvnw